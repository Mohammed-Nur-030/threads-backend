import {typeDefs} from './typedef'
import {mutations} from './mutations'
import {resolver} from './resolvers'
import {queries} from './queries'

export const User={typeDefs,queries,mutations,resolver}